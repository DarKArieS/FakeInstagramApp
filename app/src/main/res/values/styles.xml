<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/lightText</item>
        <item name="colorAccent">@color/lightText</item>
        <item name="android:textColor">#000000</item>
        <item name="android:colorEdgeEffect">@color/lightText</item>
    </style>

    <!-- Bottom Navigation theme. -->
    <style name="BottomNavigation" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#000000</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
